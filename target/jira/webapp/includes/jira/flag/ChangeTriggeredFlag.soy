{namespace JIRA.Templates.Project.ChangeTriggeredFlag}

/**
 * @param message
 * @param revertMessage
 **/
{template .fieldRevertWarning}
    <p>{$message}</p>
    <p>
        <a href="#" class="cancel">
            {$revertMessage}
        </a>
    </p>
{/template}
